# Agent Notes

- Babylon draws; the existing Trigger Rally GPL simulation/vehicle code drives the car.
- Keep physics, controls and race rules separate from original visual assets. Do not change driving behaviour as a side effect of art work.
- All shipped art, terrain samples and music are generated by this project. Do not reintroduce Trigger Rally Content (meshes, images, heightmaps).
- `static/` is the only public asset directory; historical ignored `public/tr/` must never enter a build.
- Keep `SOURCE_REVIEW.md` current when driving code, track data or licensing changes.
- Do not commit `dist/`, Playwright reports or other generated outputs.
